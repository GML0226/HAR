seed: 20260715
data:
  train_manifest: artifacts/manifests/train_manifest.csv
  test_manifest: artifacts/manifests/test_manifest.csv
  modality: Depth_Color
  image_size: 112
  frames: 4
  num_classes: 40
split:
  validation_users: [user3, user5, user19, user24]
training:
  epochs: 1
  batch_size: 4
  num_workers: 0
  backbone_learning_rate: 0.0001
  head_learning_rate: 0.0005
  weight_decay: 0.0001
  amp: true
  freeze_backbone_epochs: 1
  early_stopping_patience: 1
  max_train_samples: 32
  max_validation_samples: 16
model:
  pretrained: true
  dropout: 0.2
output_dir: outputs/resnet18_depth_smoke
